apiVersion: v1
kind: Service
metadata:
  name: homarr
  labels:
    app: homarr
spec:
  type: NodePort 
  ports:
  - port: 80
    targetPort: 7575
    nodePort: 32000 
    protocol: TCP
    name: http
  selector:
    app: homarr
