server:
  dev:
    enabled: false

  securityContext:
    runAsUser: 100
    runAsGroup: 1000
    fsGroup: 1000
    
  dataStorage:
    enabled: true
    size: 10Gi
    storageClass: "local-storage"
  ui:
    enabled: true
    serviceType: "NodePort"
    nodePort: 30082